module(
    name = "my_project",
    version = "1.0.0",
    compatibility_level = 1,
)

# Define dependencies
bazel_dep(name = "rules_nodejs", version = "3.3.0")
# Remove or update the version of rules_js if necessary
# bazel_dep(name = "rules_js", version = "1.0.0")
