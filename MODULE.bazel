module(
    name = "atlas",
    version = "1.0.0",
)

npm = use_extension("@npm_bazel/typescript:index.bzl", "npm")
npm.npm_translate_lock(
    name = "npm",
    lockfile = "//:package-lock.json",
)

use_repo(npm, "npm")
