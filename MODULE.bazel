module(
    name = "atlas",
    version = "1.0.0",
)

bazel_dep(name = "aspect_rules_js", version = "1.0.0")

load("@aspect_rules_js//js:extensions.bzl", "npm")

npm.npm_translate_lock(
    name = "npm",
    pnpm_lock = "//:package-lock.json",  # or yarn_lock for yarn.lock
    package_json = "//:package.json",
)

use_repo(npm, "npm")
